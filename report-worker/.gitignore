node_modules/
.wrangler/

# `wrangler dev` reads local secrets from here — RATE_LIMIT_SALT among them. Never commit it.
.dev.vars
.dev.vars.*
# Local-only config used to run the Worker under `wrangler dev` with the KV binding attached; the
# committed wrangler.toml ships that block commented out on purpose (see the note there).
wrangler.local.toml
# Build output from `wrangler deploy --dry-run --outdir`.
dist/
