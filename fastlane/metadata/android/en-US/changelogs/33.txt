First public release (1.0.15).
• OpenAI-compatible /v1/chat/completions (SSE) + tool calling
• Anthropic-compatible /v1/messages
• Audio in and out: transcriptions and on-device TTS speech
• Embeddings, rerank, RAG and batch endpoints
• Image generation on the full build
• Curated models + HuggingFace search; per-request model swap
• Auto-start, Doze survival, crash/OOM recovery, thermal backpressure
• Encrypted API key, HTTPS on the LAN, mDNS discovery
