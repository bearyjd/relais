Codemap refresh — 2026-08-04
Previous generation: 2026-07-19 @ main ab345ff
This generation:     2026-08-04 @ main afc237c1 (~16 days, +44 commits)

FILES SCANNED: 318 main .kt (was 334) — a NET DECREASE. 29 verified-dead files were removed
across 5 gated PRs (0c84a125 + siblings), implying ~13 net-new files in the same window.
ui/ 114 -> 90, customtasks/ 43 -> 38.

=== CHANGE SUMMARY BY FILE ===

frontend.md   74% changed  (only file over the 30% gate; rewrite approved by the user)
  - RESOLVED: the "29 genuinely dead files" section described work that HAS SINCE SHIPPED.
    Every file it listed is gone from disk (verified individually). Section replaced with a
    4-line "Dead code — cleared" note.
  - CONDENSED: the 15-line same-day CORRECTION block collapsed to a 4-line standing warning.
    The durable lesson is kept deliberately: Hilt @IntoSet multibinding is invisible to an
    import-graph BFS, which is what produced the wrong "157 files dead" claim on 07-19. The
    file now carries that lesson so the mistake is not re-derived; the incident narrative is
    dropped.
  - KEPT: ui/home/LicensesActivity.kt survived the cleanup (manifest-declared, probe-covered).
    Its launch path SettingsDialog did NOT, so it is manifest-live but unreachable — corrected
    from "IS dead" (present tense, pre-cleanup) to "was removed by the cleanup".
  - CORRECTED: customtasks/agentchat/ 22 -> 24 files (stale count, unrelated to the cleanup).

architecture.md   27% changed
  - CORRECTED: RelaisHttpServer 1876L -> 2202L, RelaisEngine 782L -> 1026L (both materially
    stale; the file counts and line sizes were the least trustworthy part of the old map).
  - CORRECTED: the dead-code pocket is removed, not pending. Title framing updated to match.
  - ADDED: the #220 runtime-compat gate to the resilience spine — four chokepoints over one
    measured table (catalog / provisioner x2 / per-request swap / legacy Gallery download).

backend.md   23% changed
  - ADDED MISSING ROUTE: POST /v1/messages (Anthropic Messages API, #179). Shipped ~07-23/25,
    i.e. AFTER the last refresh, and never made it into the route table. Handler is live at
    RelaisHttpServer.kt:484 (dispatch) / :1372 (handleAnthropicMessages).
  - ADDED: a runtime-compat gate section — chokepoint table, the VERIFIED/INCOMPATIBLE/SUSPECT/
    UNKNOWN axis, the independent license-gating axis, and where the operator-facing vs
    API-client refusal copy each live (refusalMessage vs incompatibleModelMessage).
  - CORRECTED: RelaisHttpServer/RelaisEngine line counts (as above).

dependencies.md   15% changed
  - ADDED: appId != namespace. build.gradle.kts onVariants sets applicationId per channel while
    namespace stays cc.grepon.relais. This is the root cause of the stale probe instrument
    targets fixed in PR #245, so it is now recorded where a reader will look.
  - ADDED: v1.0.16 (2026-08-04) is the FIRST published GitHub Release, with asset sizes and the
    R8-is-on-but-not-in-CI caveat.
  - VERIFIED UNCHANGED: litertlm 0.12.0, llmedge 0.4.7.2, flavor matrix, license-lint CI.

data.md   13% changed
  - ADDED: the #180 model registry (provisioned_models plaintext pref -> ProvisionedModel
    triples). Previously undocumented despite being the SAFETY BOUNDARY for per-request model
    swaps, and despite being pruned-on-read rather than on-write. That read-time pruning is
    load-bearing and was invisible in the old map.
  - VERIFIED UNCHANGED: Room v5, 5 DataStore serializers, Hilt AppModule, provisioning paths.

=== STALENESS WARNINGS ===
- None of the 5 maps were 90+ days stale (16 days since last full pass).
- HOWEVER, two files had drifted WITHOUT their header reflecting it: dependencies.md and
  frontend.md were content-edited on 2026-07-28 (llmedge bump, #211 speech) while their
  <!-- Generated: 2026-07-19 --> header stayed put. Freshness headers are only trustworthy if
  every content edit bumps them; a reader checking the header would have been misled by 9 days.
  Worth a lint if this recurs.
- The line-count facts (1876L, 782L, 22 files) rotted fastest and silently. They are the
  cheapest thing to get wrong and the easiest to auto-verify — a candidate for generation
  rather than hand-maintenance.

=== NOTE, NOT A CODEMAP ISSUE ===
Android/src/app/src/main/java/cc/grepon/relais/.omc/state/ exists inside the Kotlin source
package. It is untracked (0 files known to git) so it does not ship, but it is OMC tooling state
sitting in a source tree. Harmless today; worth a .gitignore entry or relocation so it is never
picked up by a glob-based scan.
