<?xml version="1.0" encoding="utf-8"?>
<!-- Relais home-screen widget (#3): a quick-prompt surface backed by the Glance RelaisWidget.
     initialLayout is Glance's bundled loading layout (shown until the first provideGlance render).
     Resizeable both ways with a small min footprint; targetCell sizing is API 31+ (minSdk is 31). -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="180dp"
    android:minHeight="110dp"
    android:targetCellWidth="3"
    android:targetCellHeight="2"
    android:minResizeWidth="180dp"
    android:minResizeHeight="110dp"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen"
    android:description="@string/relais_widget_description"
    android:initialLayout="@layout/glance_default_loading_layout"
    android:previewLayout="@layout/glance_default_loading_layout" />
